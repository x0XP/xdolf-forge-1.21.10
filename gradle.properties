# Sets default memory used for Gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx5G
org.gradle.daemon=false

# In the case that Gradle needs to fork to recompile, this will set the memory for that process.
systemProp.net.minecraftforge.gradle.repo.recompile.fork=true
systemProp.net.minecraftforge.gradle.repo.recompile.fork.args=-Xmx5G

# Opts-out of ForgeGradle automatically adding mavenCentral(), Forge's maven and MC libs maven to the repositories block
systemProp.net.minecraftforge.gradle.repo.attach=false


## Environment Properties

# The Minecraft version must agree with the Forge version to get a valid artifact
minecraft_version=1.21.10
# The Minecraft version range can use any release version as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly.
minecraft_version_range=[1.21.10]
# The Forge version must agree with the Minecraft version to get a valid artifact
forge_version=60.1.0
# The Forge version range can use any version of Forge as bounds or match the loader version range
forge_version_range=[60.1.0,61)
# The loader version range can only use the major version of Forge/FML as bounds
loader_version_range=[60,61)
# The mapping channel to use for mappings.
# The default set of supported mapping channels are ["official", "snapshot", "snapshot_nodoc", "stable", "stable_nodoc"].
# Additional setup is needed for parchment mappings, see https://parchmentmc.org/docs/getting-started
mapping_channel=official
# The mapping version must match the Minecraft version.
mapping_version=1.21.10


## Mod Properties

# The unique mod identifier.
mod_id=xdolf
# The human-readable display name.
mod_name=Xdolf
# License.
mod_license=GPL-3.0-only
# Development build version.
mod_version=4.0.0-dev.12
# Group ID.
mod_group_id=com.darkcart.xdolf
# Authors.
mod_authors=x0XP, Sgt Pepper, original Xdolf contributors
# Description.
mod_description=Xdolf client port to Forge 1.21.10. Development build; see PORTING.md for feature coverage.
